spring:
  cloud:
    config:
      name: gateway
      profile: native
      label: master
      discovery:
        enabled: true
        service-id: service-center

eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:10000/eureka
